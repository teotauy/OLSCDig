# Self-Hosted Wallet & Check-In Plan

Planning doc for replacing PassKit with an OLSC Brooklyn-owned membership pass, check-in, and season analytics system.

## Goal

Replace the expensive PassKit dependency with a system we own:

- Members live in our database.
- Check-ins live in our database.
- Apple Wallet / Google Wallet passes are generated by us.
- Admins can scan members through a web page.
- Season exports and dashboards come from our own data.

PassKit becomes unnecessary once Wallet generation, pass links, updates, and check-ins are handled here.

## Status (last consolidated Aug 31 — see gate table for latest per-item dates)

**Aug 30–31 update:** Forest → Ipswich was the first real next-match
cycle after the Aug 24–29 protocol work. GitHub Actions ran: Aug 29
cron detected the change and reported Apple `99/99` APNs 200s; Aug 30
no-op'd because the fingerprint was already saved. APNs 200 is not
"the phone has the new pass." Fixes shipped the same day (Apple's
current APNs headers, safer 304, `last_fetched_at` retry, Google PATCH
includes logo, football-data.org `SCHEDULED`+`LIVE` instead of
comma-status `limit: 25`, admin push in a background thread so it
doesn't 502). The limit-25 bug briefly painted new passes with Spurs
12/19 — not an override (`match_overrides` was empty). **Manual push
confirmed Aug 31** on real phones (Will Foote's Saturday pass showed
Ipswich). Production: **85** registered Apple passes / **100**
devices, all fetched; **38** pre-`webServiceURL` still cannot register;
barcode still scans. Unattended cron after the *next* fixture is the
remaining watch. Pass Remediation is still a deliberate hold (in-person
/ Discord, not a blast). Full write-up:
[QA_VERIFICATION_PLAN.md](QA_VERIFICATION_PLAN.md) and
[MATCH_UPDATES_SETUP.md](MATCH_UPDATES_SETUP.md).

**Aug 24-29 update:** the first real match (Newcastle, Aug 23, ended in a
draw — auto-synced correctly) surfaced a run of real, previously-hidden
bugs, all found and fixed the same way as Aug 20's: verified against
real devices/logs, not assumptions. In order: a full code-level security
review found and fixed an open redirect, a Google OAuth flow that failed
*open* (granted admin access if the userinfo call errored, skipping the
email allowlist), and two timing-unsafe secret comparisons — also
surfaced that `FLASK_SECRET_KEY` was unset in Render this whole time
(hardcoded fallback in use), fixed by setting a real value after
migrating the 138 existing encrypted tokens to it first so nothing broke.
Built revocable, scanner-only door-access links for volunteer staff
(previously they'd have needed the full admin password). Then, testing
the owner's own installed pass turned up a real Apple Wallet bug:
`relevantDate` makes a pass show as "Expired" the instant it's in the
past, and ours was tied to a manually-managed field nobody updates —
removed entirely, added a real season-long `expirationDate` instead.
Triggering the resulting push to confirm the fix woke ~95 devices at
once and **crashed the app for real** — Flask's dev server couldn't
absorb the burst, compounded by every device independently (uncached)
hitting football-data.org and tripping its rate limit. Fixed both
(gunicorn, 120s fixtures cache) and re-verified by triggering the exact
same scenario again — 96/96 pushed, app stayed up. Also found and fixed:
a second, redundant push firing on every "set current match" action
(leftover coupling from before the relevantDate fix), and the venue's
GPS coordinates being off by ~220m (real matchday test: zero lock-screen
alerts) — corrected via real geocoding, not yet re-tested in person.
One deliberate non-action: the ~38 members with pre-fix stale passes
have still not been sent Pass Remediation — see
[QA_VERIFICATION_PLAN.md](QA_VERIFICATION_PLAN.md) for why and what's
next.

**Aug 20 update:** a real production bug shipped and got caught the hard
way — the Apple Wallet `webServiceURL` had a doubled `/v1` segment,
silently 404ing every real device's push-registration attempt since the
feature shipped (Aug 17). Root cause, fix, and the resulting QA overhaul
are covered in detail in the new gate-table row below and in
[QA_VERIFICATION_PLAN.md](QA_VERIFICATION_PLAN.md), which now tracks
every external integration's real verification status going forward.
Same day: shipped `If-Modified-Since`/304 support for the PassKit
web-service (a real spec gap found during that audit, not the bug
itself), mothballed the remaining PassKit-vendor-API admin routes behind
a code gate, built two new bulk pass-sending admin pages, added
automatic Google Wallet match-week updates (the same push-on-change
behavior Apple already had), and fixed a second real bug the first real
production email send exposed (Resend's rate-limit reset time was being
shown as if it were the daily quota's reset time — it isn't, and Resend
exposes no reset time for the daily/monthly quota at all). By end of day,
all 85 members without a pass had real first-time passes sent
successfully. The 40 pre-fix stale passes were **not** resent that day
(a previous version of this doc claimed they were) — 38 of those
still exist as of Aug 31; see the pass-remediation row and
[QA_VERIFICATION_PLAN.md](QA_VERIFICATION_PLAN.md).

**Corrected runway (Aug 10):** the Alamo checkpoint moved from Aug 12 to
**Wednesday Aug 19** — the real constraints are Aug 21 (last work day before
travel) and Aug 23 (first match, Newcastle), not the placeholder Aug 17-18
target this doc originally guessed at. That's ~11 days of work time from
today, not ~2. Both wallet platforms are now proven end-to-end (Apple Wallet
in production since Aug 10, Google Wallet confirmed with a real successful
save-to-wallet flow on Aug 15) — everything left is predictable web-app work
(export, real-device confirmation, Google's own approval timeline), not open
questions about feasibility.

Gate-by-gate (see full list under "Go/No-Go Gates" below):

| Gate | Status |
| --- | --- |
| Production HTTPS URL | Verified for hosted Wallet pass download on Render; not yet re-verified with new DB/admin code deployed |
| DB survives deploys/restarts | Infrastructure done (Supabase live, schema applied); not yet exercised by real check-in traffic |
| Admin login works in production | Presumed OK, unchanged by this work |
| Members can be added/imported | Done and tested locally; **not yet deployed to Render** |
| Real Apple Wallet pass on 2 iPhones | **Done (Aug 10): production `/wallet/test-pass.pkpass` on Render generates a real signed pass with live next-match data, confirmed working end-to-end.** Still worth testing install on a 2nd phone before the checkpoint, but the hard part (prod signing) is proven. Icon/logo art also upgraded (Aug 10) from generated placeholder text to the real OLSC/LFC crest — see "Real crest art" note under Apple Wallet below. |
| Mobile web pass page (Android path) | **Done (Aug 10, extended later same day):** animated ring around the QR (motion confined to the ring only via `overflow: hidden` + oversized rotating conic-gradient — confirmed via two screenshots that the ring moves while the QR itself is pixel-identical), automatic home/away theming matching the Wallet pass (same `is_home` source as "Next Match" text, so they can't disagree — verified against the real live fixture, which is actually away), and the real crest+wordmark image now shown on the page too (previously text-only — no platform reason for that, just an oversight). `GET /pass/<token>` built and verified — looks up by hashed token (`db.find_active_wallet_pass_by_token`), shows name/season/live next-match/QR, generic 404 for invalid/expired tokens (no info leak). Wired into the resend-pass email so it's actually reachable, not just a route that exists. Tested via Flask test client (valid + invalid token) and confirmed visually in a real browser. Still needs production deploy + a real non-iPhone browser test. |
| Scanner scans the QR | **Done (Aug 10): deployed to Render and scanning in production**, not just locally. |
| Fresh scan creates a check-in | **Done (Aug 10): confirmed working on Render** against live Supabase data. |
| Duplicate scan shows "already used" | **Confirmed (Aug 15).** `checkins` has `UNIQUE (member_id, match_id)`; the scan endpoint does `INSERT ... ON CONFLICT DO NOTHING RETURNING` — a fresh insert returns `checked_in`, a conflict falls back to looking up the existing row and returns `already_checked_in` with its *original* timestamp. Verified with a real functional test (not just code review): two scans of the same token against the real current match, both timestamps identical, second scan correctly flagged as a duplicate. Tested against Supabase directly. |
| Google Wallet demo-mode links | **Confirmed fully working end-to-end (Aug 15).** Root cause of the whole Aug 13 debugging saga: `GOOGLE_WALLET_ISSUER_ID` was set to `3388000000023170524`, a leftover from an earlier account mixup — the real, authoritative issuer (confirmed live in the Google Pay & Wallet Console under the correct OLSC Brooklyn login) is `3388000000023188178`. Every "Permission denied" was the service account correctly being denied access to an issuer it had nothing to do with; it was never a JWT/payload/cert problem (all of those were separately verified correct along the way). Fixed by updating `GOOGLE_WALLET_ISSUER_ID` in `.env` and Render. Verified via direct Google Wallet REST API calls (GET returned a clean 404 "class not found" instead of 403 once the ID was fixed — confirming real authorization — then a POST successfully created the real production class, status 200) and then via an actual save-to-wallet flow on desktop Safari: a real pass now lives in Google Wallet with the correct crest, season, and live next-match data, QR scannable. Still shows `[TEST ONLY]` — that's expected, demo/publishing-access mode. **Approved (Aug 18):** Google emailed confirmation that OLSC Brooklyn is approved for real Passes access in the Google Pay & Wallet Console — no longer test-account-only. In response: the Google Wallet link, hidden from the pass email since Aug 16 while it only worked for approved test accounts, is back — as a secondary "or add it straight to Google Wallet" link underneath the primary (guaranteed-to-work-for-everyone) mobile pass button in the Android box, not competing with it as a second primary CTA. Verified via a captured-HTML test: the link renders when a `google_wallet_url` is present and is cleanly absent when it isn't (e.g. if that build ever fails for one member). **Confirmed (Aug 18):** opened a real save link for a real member post-approval — clean standard "Add pass" screen, no `[TEST ONLY]` badge, no demo-mode text anywhere. No console setting needed flipping; the approval took effect on its own. **Still open:** confirming the same on an actual Android device tapping "Add" through to a saved pass (the screen itself has been confirmed clean; the full save action on a real phone is the one link not yet directly observed). |
| Last-match/check-in CSV export | **Done (Aug 15).** `GET /admin/matches/<id>/export-checkins.csv` (name/email/phone/checked-in time/source for one match — "Export CSV" link per row on `/admin/matches`) and `GET /admin/members/export.csv` (current-season roster with each member's check-in count — "Export CSV" link on `/admin/members`). Verified against real production Supabase data, not test rows — the match export correctly included a real check-in from earlier scanner testing. |
| Documented resend flow | Built and deployed; pass/report email now uses Resend first with SMTP fallback. Render has `RESEND_API_KEY`; default sender is `OLSC Brooklyn <DIGITALIDS@OLSCBROOKLYN.COM>` and default reply-to is `OLSC_BK@olscbrooklyn.com`. **Redesigned (Aug 11):** the fulfillment email (`_send_pkpass_email`'s HTML, shared by both the Resend and SMTP paths) no longer uses a generic red/green gradient + soccer emoji — now uses the real crest+wordmark image and a proper red CTA button for the Android/mobile-pass link, matching the pass and web page design language. Verified by rendering to a local file and viewing in-browser (not sent as a real test email, since Resend/SMTP are both live now). **Rewritten again (Aug 16)** per feedback that members don't read emails carefully: replaced the single combined instruction block with two bold, clearly-separated device-specific boxes — an iPhone box (`.step-label` in bold red caps: "ON IPHONE") and an Android box ("ON ANDROID"), each with its own plain-language instruction and its own red CTA button, plus an explicit "Do not open the attachment — it will not work" warning in the Android box (the `.pkpass` attachment silently does nothing on Android, which was a real confusion risk). The Google Wallet button was removed from the rendered email (the function still accepts a `google_wallet_url` argument for signature compatibility, it's just not shown) — see the kill-switch row below for why. Verified via Flask test-client rendering + HTML source inspection (browser tool was unavailable this session). |
| Email sending kill switch (`EMAIL_SENDING_ENABLED`) | **Built (Aug 16)**, not yet activated on Render. Per explicit instruction — "we're not sending these out until the google stuff is approved and real" — `_send_pkpass_email` now checks `os.getenv('EMAIL_SENDING_ENABLED', 'true')` first and returns `False` immediately (logging to stdout, sending nothing) when it's set to `false`. The pass/token is still generated and stored either way — only the email is suppressed — so admins can still use "Download Pass" to hand someone a `.pkpass` file directly while sending is paused. `_issue_and_email_pass`'s admin-facing error message now distinguishes "deliberately paused right now (EMAIL_SENDING_ENABLED=false) — not a bug" from a real SMTP/Resend failure, so this doesn't get mistaken for breakage. Verified end-to-end through the real `/admin/members/<id>/resend-pass` route (not a standalone script) with the var set to `false`: pass issued, email correctly blocked, admin message correctly worded. **Activated (Aug 16):** `EMAIL_SENDING_ENABLED=false` set in Render's environment tab — real pass emails were paused. **Gate cleared (Aug 18):** Google approved real Passes/Wallet publishing access, which was the specific condition for lifting this. Google Wallet button re-added to the email in the same change (see row above). **Still needed:** actually flip `EMAIL_SENDING_ENABLED` to `true` (or delete the var) in Render's environment tab — the code change doesn't do this by itself, same as it didn't turn sending off by itself back on Aug 16. |
| Live check-in count on the scanner | **Built (Aug 18).** Not a live occupancy tracker — there's no "check out" concept in this system, checkins are a permanent per-match record, not a togglable present/absent state (confirmed this is fine: a member checked into two matches stays checked into two matches forever, regardless of how many times their pass gets reissued in between). This is just a running count of check-ins for the current match, shown on `/scanner`, so door staff can watch for fire-marshal capacity on early-entry nights. New `GET /api/checkins/count`; refreshes on the existing `headcount_refresh_seconds` interval plus immediately after every new check-in. Verified against real production data via Flask test client. |
| Season check-in leaderboard | **Built (Aug 18).** 3 points for a checked-in win, 1 for a draw, 0 for a loss or an unplayed/unsynced match — points only come from matches a member actually checked into, which matters since we've never run the scanner for every match; that's fine by design, it just means fewer scoring chances, not a penalty. Match results are synced automatically rather than entered by hand: a new `get_finished_liverpool_matches()` pulls football-data.org's finished-matches feed, matched to our own `matches` rows **by kickoff date alone** (Liverpool plays at most once a day, so date is unambiguous — no fragile opponent-name string matching needed). Result (win/draw/loss) is computed directly from the raw `score.fullTime` home/away goal counts rather than trusting the API's own `winner` field, since I couldn't fully verify that enum's exact values against official docs and didn't want a wrong guess silently baked into a scoring system. New `POST /internal/sync-match-results` (same shared-secret pattern as the existing daily job) added as a second step in the same GitHub Actions schedule — reuses infrastructure rather than standing up a separate cron. Admin-only `/admin/leaderboard`, linked from the shared footer. **Verified for real:** schema applied to production Supabase; the full match → result → leaderboard-points chain tested end to end against real production data (a synthetic finished match inserted, synced, and confirmed to award exactly 3 points to a real checked-in member, then cleaned up); route auth tested (401 without the secret); confirmed `get_finished_liverpool_matches()` correctly returns zero results right now, which is expected — no match has been played yet this season. **Nothing to verify further** — this will just start populating naturally as real matches get played and synced, no action needed. |
| Official crest art, #E31B23 red, header layout fix | **Built (Aug 18).** Three related asks. (1) Logo assets replaced with the official club artwork (`Desktop/logos/`) — crest cropped cleanly from the stacked crest+text lockup, wordmark sourced from the wide crest-beside-text lockup, red variants recolored pixel-exact to `#E31B23` (white variants used as-is). (2) The pass logo wasn't actually left-alignable before this — the wordmark source file has its artwork centered inside a canvas far wider than the content, so `_load_art` now trims every asset to its real content bounding box first, and a new `_paste_left_aligned` (vs the old `_paste_centered`) pastes the logo flush-left. (3) "Next match" moved from `secondaryFields` (only visible once a pass is expanded) to `headerFields`, which Apple keeps visible even when the pass is collapsed in a stack — previously-unused real estate. Every `#c8102e`/`rgb(200,16,46)` across the *active* app (wallet pass, Google Wallet, all current admin/member templates, the email) replaced with `#e31b23`/`rgb(227,27,35)`; legacy PassKit-era pages and dev scratch files deliberately left untouched. **Verified for real:** built an actual pass and confirmed `headerFields` carries live next-match text, colors are exactly `rgb(227,27,35)`, and the rendered `logo.png` has an asymmetric left/right margin (9px vs 17px) confirming true left-alignment; all active pages smoke-tested post-recolor with no rendering errors. |
| Automatic member signup from Squarespace orders | **Built (Aug 17).** Squarespace's Core plan has no native webhooks (confirmed: no Developer Platform option under Settings), so a real Squarespace-side webhook wasn't an option. Zapier was the obvious middle-man but its free plan excludes webhooks entirely (needs Professional, $19.99-29.99/mo — confirmed against Zapier's own pricing page, not assumed); **Make.com's free plan (1,000 ops/month) does support webhooks and has a native Squarespace "Watch Orders" trigger**, so that's the path. New endpoint `POST /api/squarespace/order` — authenticated via a shared-secret header (`X-Webhook-Secret`, env var `SQUARESPACE_WEBHOOK_SECRET`), since Make can't hold an admin session cookie. Reuses the exact same `_upsert_member_in_season`/`_issue_and_email_pass` helpers the "Add Member" admin form already uses, so a Squarespace order behaves identically to an admin manually adding that person — including respecting `EMAIL_SENDING_ENABLED`. Idempotent against retried/duplicate webhook deliveries via a new `squarespace_orders_processed` table keyed on Squarespace's own order `id` (same `INSERT ... ON CONFLICT DO NOTHING RETURNING` idiom already used for check-ins) — a duplicate delivery no-ops instead of re-emailing a welcome pass. **Verified for real:** schema applied to production Supabase; full flow tested via Flask test client — wrong/missing auth rejected, missing fields rejected, valid order creates the member + attaches to season + issues a pass (confirmed present in the real `members` table), duplicate `order_id` correctly no-ops, and the `EMAIL_SENDING_ENABLED=false` kill switch correctly suppressed the email while still generating the pass, all against real production data (then cleaned up afterward). **Confirmed via Squarespace's own developer docs** (not assumed) that order objects use `id`, `customerEmail`, `billingAddress.firstName`, `billingAddress.lastName`, `billingAddress.phone`, and `lineItems[].productName` — those are the fields to map in the Make scenario. **Still needed:** the actual Make.com scenario hasn't been built (that's on Squarespace/Make's side, not code) — see the setup steps below — and `SQUARESPACE_WEBHOOK_SECRET` needs to be set on Render. **Deliberately not built:** server-side filtering by product name (to only fire for the membership product, not any other shop item) — recommended to do that as a Filter step inside the Make scenario itself instead, so it stays adjustable without a code change if the product listing changes. |
| Automatic pass delivery on member add | **Done (Aug 10):** adding a member via `/admin/members` now automatically issues a token, builds a real signed pass, and emails it — same underlying logic as "Resend Pass" (extracted into a shared `_issue_and_email_pass` helper). Deliberately scoped to the single-add form only, not CSV bulk import (importing 100 rows shouldn't silently fire 100 emails). Verified: pass/token get created even when email fails, and the failure is surfaced clearly to the admin rather than silently swallowed. |
| Apple Wallet push updates (PassKit web service) | **Built (Aug 17); protocol corrected Aug 30; real phones confirmed Aug 31.** Root problem: an already-issued `.pkpass` is a static file — "Next Match" and home/away theme were baked in once at issue time and never changed again. `pass.json` includes `webServiceURL`/`authenticationToken`; `pass_devices` tracks registrations; `/passkit/v1/*` implements Apple's spec. Rebuilds keep the **same** barcode (`token_encrypted`). APNs uses the Pass Type ID cert over HTTP/2. **Trigger points as of Aug 30:** daily GitHub Actions job + **Push Pass Updates Now** (background thread). **Set current match does not push** (scanner-only since Aug 25). **Aug 30 protocol (Apple's current APNs docs, not the 2018-era pass-update sample):** `apns-push-type: background`, `apns-priority: 5`, empty `{}` body, one reused HTTP/2 connection. Invalid tokens dropped from `pass_devices` only — that is not deleting the Wallet pass. `If-Modified-Since` 304 only on an exact Last-Modified match (Wallet sending "now" was 304'ing devices that never fetched). `last_fetched_at` + cron `retry_unfetched`. Fingerprint stamped after notify. **Verified for real Aug 31:** Will Foote's installed pass showed Ipswich after a manual push; 85 registered passes / 100 devices, all fetched. `FLASK_SECRET_KEY` was set on Render Aug 24–25 (encrypted tokens migrated). Remaining: unattended cron after the next fixture; the 38 pre-fix files still cannot register. |
| Database-backed match overrides + auto-push on change | **Built (Aug 17).** Two related problems fixed together. (1) Next-match / cup-tie overrides moved from `match_overrides.json` (ephemeral on Render) to a `match_overrides` DB table and `/admin/match-overrides`. `get_next_match()` merges overrides by date; a far-future cup row cannot skip an earlier Premier League game; `is_home` on the override drives pass color. (2) `/internal/check-next-match` fingerprints the current next match and auto-pushes on change. GitHub Actions (`.github/workflows/check-next-match.yml`) hits it daily at 9am UTC (schedule was tightened from every 3 hours). **`INTERNAL_TASK_SECRET` is working in production** — the Aug 29 cron returned `changed: true` (Forest→Ipswich), not 401. Unchanged fingerprints now retry devices that never fetched (`reason: retry_unfetched`) instead of treating APNs 200 as done. Adding an override still does not push by itself — click **Push Pass Updates Now** or wait for the job. See [MATCH_OVERRIDES.md](MATCH_OVERRIDES.md). |
| Fixed doubled `/v1` breaking all real Apple push registration | **Found and fixed Aug 20.** `webServiceURL` in `pass.json` already had `/v1` baked into the base, and Apple's client automatically appends `/v1/devices/...` itself — every real device's registration attempt since Aug 17 silently 404'd. Never caught earlier because verification up to that point checked the code's internal consistency, never Apple's actual documented client behavior. Fixed in `wallet_pass.py`'s URL construction; every real member issued a pass before this fix has a permanently-broken push path baked into that specific `.pkpass` and needs a fresh one — see the pass-remediation row below. Triggered a full QA audit of every other external integration, tracked from here on in [QA_VERIFICATION_PLAN.md](QA_VERIFICATION_PLAN.md). |
| `If-Modified-Since`/304 support for the PassKit web service | **Built Aug 20; equality check Aug 30.** The original gap: get-latest-pass always returned 200. Aug 20 added 304, but used `If-Modified-Since >= Last-Modified`. Wallet often sends "now," so devices 304'd without fetching the new pass. **Aug 30:** 304 only if the client date matches the current content tag within 2 seconds. A genuine 304 still stamps `last_fetched_at`. Tests in `tests/test_pass_update_protocol.py`. |
| Legacy PassKit-vendor-API routes mothballed | **Built Aug 20.** Per explicit instruction to keep the code intact but hidden rather than delete it. `_passkit_legacy_enabled()`/`_passkit_legacy_gate()` in `app.py` gate the 6 remaining routes that call PassKit's own vendor API (`/legacy/passkit/add-member`, `/update-match`, `/resend-welcome`, and their `/api/*` POST targets) behind `PASSKIT_LEGACY_ENABLED` (default off) — they now 404, not 403, so they look genuinely absent rather than locked. Deliberately does **not** touch `/api/next-match` or any `/passkit/v1/*` route — those are unrelated live systems (our own webservice protocol, not PassKit's API). Verified all 6 return 404 by default, 200 with the flag on, and that both unrelated systems are unaffected either way. |
| Pass-remediation bulk tool (`/admin/pass-remediation`) | **Built and verified Aug 20; sending still a deliberate hold as of Aug 31.** Direct response to the `/v1` bug: reviewable checkbox page of members with an active Apple pass issued before the fix (`db.get_apple_passes_issued_before`), test/dev accounts unchecked by default, CSV export, send gated behind an explicit JS confirm. Verified with a fake sender before any real email. **Correction:** an earlier version of this row said all 40 were sent Aug 20. That never happened — checked against the DB Aug 25 and again Aug 31: **38** pre-fix stale passes still exist. `issue_wallet_token`'s `ON CONFLICT` path *was* fixed Aug 20 to bump `created_at` on rotation so a resent member drops off the list. Those 38 cannot register for push; barcode still scans. Resend is in-person / Discord opt-in, not a blast. |
| Issue-first-passes bulk tool (`/admin/issue-passes`) | **Built and verified Aug 20.** Same reviewable-checkbox/CSV/explicit-confirm pattern as pass-remediation, for the opposite cohort: the 85 real members who had never been issued a pass at all this season (confirmed via `db.get_members_without_wallet_pass`, season-scoped). Verified the send route correctly refuses to act on a member_id outside the real current snapshot (tested against someone who already had a pass — silently skipped, not double-sent). **Used for real Aug 20:** all 85 sent successfully — confirmed against real `wallet_passes` rows, not just the on-screen success message. |
| Google Wallet automatic match-week updates | **Built Aug 20; logo included Aug 30.** `google_object_id`/`google_class_id` persisted at issuance; `_notify_google_pass_updates()` PATCHes issued objects on the same triggers as Apple (daily job + **Push Pass Updates Now**; set-current no longer pushes). **Aug 30:** PATCH now includes `logo` with `hexBackgroundColor` and `textModulesData` — color-only left the previous match's wordmark on the new background. **Verified against Google's live API** (insert-then-patch-then-independent-GET) and at production scale (Aug 25: 12/133 200s, rest expected 404s; Aug 29: ~15/135). `google_object_id` means we generated a save link, **not** that they saved it. `platform` is always `apple` — we cannot count Android devices. Still nobody has personally confirmed content on a real Google Wallet screen. |
| football-data.org next-match pagination trap | **Found and fixed Aug 30.** Comma-combined statuses + `limit: 25` returns the *last* 25 of the season (live proof: Spurs Dec 19 on every new pass). `status=SCHEDULED` alone starts at the real next fixture (results labeled `TIMED`). Query is now `SCHEDULED` (limit 50) + separate `LIVE` (limit 10), merge by id, keep local calendar date ≥ today. Ipswich Town FC abbreviation added. Overrides table was empty — not a cup override. |
| Admin push 502 / gunicorn timeout | **Found and fixed Aug 30.** **Push Pass Updates Now** waited in-request for the full fan-out; gunicorn `--timeout 30` 502'd. Button now starts a daemon thread and redirects with "Update started…". `render.yaml` timeout **180**. Live dashboard start command can still be 30s if the service isn't on the Blueprint — check it if 502s persist after Matches has loaded. Deploys also 502 the button. |
| Bulk-send pacing + Resend usage visibility | **Built Aug 20.** Resend's documented default cap is 10 requests/second per team; the two bulk-send routes above now share `_bulk_issue_and_email()`, pacing requests 0.3s apart so a big batch can't legitimately burst past that. The "N sent today" usage badge (previously only on the members page) now also renders on both bulk pages via the same `inject_resend_usage` context processor, extended to those two endpoints. |
| Resend reset-time bug — rate-limit window conflated with daily/monthly quota | **Found and fixed Aug 20, via the very first real send this session.** The real response headers showed `reset_at` landing 0.57 seconds after `checked_at` — confirmed against Resend's actual docs that `ratelimit-reset` is the ~1-second burst-rate window, not the daily/monthly email quota, and that Resend documents no reset-time header for the quotas at all. Previously, a real 429 from hitting the *daily* quota would have shown a failure message claiming it "resets" in a few seconds — false reassurance at exactly the moment it mattered, since the daily cap doesn't actually clear for up to 24 hours. Fixed by capturing Resend's real error `name` field (`rate_limit_exceeded` / `daily_quota_exceeded` / `monthly_quota_exceeded`) and giving each its own honest message; dropped the "resets in Xh Ym" countdown from the usage badge entirely, since there's nothing real to count down to for the case it was built for. This is exactly the kind of gap the Aug 20 QA overhaul exists to catch — found on the very first real send after adopting that standard. |

**Biggest remaining lift, as of Aug 31:** unattended daily cron after
the next fixture change, plus the 38 pre-fix passes (a when, not a how).
Manual **Push Pass Updates Now** is confirmed on real Apple phones
(Will Foote, Ipswich; 100/100 registered devices fetched). Google PATCH
succeeds at API scale (~15 real saves) but has not been visually
confirmed on an Android Wallet screen. See
[QA_VERIFICATION_PLAN.md](QA_VERIFICATION_PLAN.md).
(Historical note: this section used to list CSV/check-in export as the
biggest gap — that shipped Aug 15; then real-device confirmation, which
closed for Apple Aug 31.)

**Next up, roughly in order:**

1. **Unattended cron after the next fixture** — Forest→Ipswich was
   detected then not retried; fetch tracking is in place. Do not verify
   by rotating a real member's pass — see
   [ISSUE_PASS_VERIFICATION_MUTATES_REAL_DATA.md](ISSUE_PASS_VERIFICATION_MUTATES_REAL_DATA.md).
2. **The 38 pre-fix passes** — barcode still scans; they cannot
   register. In-person / Discord opt-in, not a bulk confession email.
   Skip `/admin/pass-remediation` unless a specific match needs everyone
   current.
3. **Google Wallet on-screen confirmation** — PATCH 200s are real; nobody
   has watched the saved pass change on a phone.
4. **Venue lock-screen alert** — coordinates corrected Aug 25, never
   re-tested in person.
5. **Squarespace → Make.com**, end to end with one real order — the
   webhook endpoint is built and tested against hand-built payloads; the
   Make scenario itself and a real purchase through it are still
   unconfirmed.
6. **Decide the fate of the 3 mothballed legacy PassKit pages**
   (`/legacy/passkit/add-member`, `/update-match`, `/resend-welcome`) —
   worth testing their real form submissions against the live PassKit
   API, or just retire them since they're unlinked and fully superseded.
7. **Mobile admin hub** — dead-simple, large-button mobile page (Scan
   People In / Look Someone Up / Add a Member / View Leaderboard). Button
   set agreed, not yet built.

`INTERNAL_TASK_SECRET` is confirmed matching (Aug 29 cron returned
`changed: true`, not 401). Leaderboard auto-sync was confirmed Aug 25
against the real Newcastle result.

**Notable already-done items, for context (not upcoming work):**

- **Self-service pass recovery** (`/recover-pass`, done Aug 15/16) — found
  in current season → issues a fresh token and emails the pass; not
  found → points at the real membership purchase link. Rate-limited.
- **Admin roster-search scanner fallback** (done Aug 16) — `/scanner`'s
  **Camera / Search** toggle searches the roster client-side (names + IDs
  only, no PII) and checks someone in by member ID directly, no QR
  needed. A zero-match search shows a QR straight to the membership shop
  page instead of a dead end — "not on the list" becomes an on-the-spot
  sale opportunity.

## Product Scope

### Must Have

- Member database for each season.
- Admin login.
- Add/import members.
- Issue each member a unique membership credential.
- Apple Wallet pass support.
- Mobile web pass page (name, season, QR code) — the actual Android path for launch, since Google Wallet is a fast-follow, not day-one. See "Decision (Aug 7)" under Google Wallet.
- QR code on each pass.
- Admin-gated scanner web page.
- Check-in history per match.
- Match management and next-match display.
- End-of-season export/dashboard.

### Should Have

- Google Wallet support.
- Squarespace import/webhook.
- Welcome email with pass links.
- Automatic pass delivery after signup.
- Self-service pass recovery by email.
- One-click last-match CSV export.
- Bulk checkout/end-match action.
- Duplicate detection by email.
- Basic audit log for admin actions.

### Not Needed Initially

- Full PassKit feature parity.
- Geofenced automatic check-in.
- Native iOS or Android scanner app.
- Complex loyalty points.
- Public member self-service beyond pass download.
- Custom domain on the launch critical path.
- Google Wallet before Apple Wallet works.
- Full migration framework/repository abstraction before the MVP works.
- Self-service pass recovery before launch.

## Recommended Architecture

### Source Of Truth

Use our database as the permanent source of truth. Pass files and Wallet providers are outputs, not the database.

Recommended tables:

- `seasons`
- `members`
- `member_seasons`
- `wallet_passes`
- `matches`
- `checkins`
- `imports`
- `admin_audit_log`

Recommended production DB: managed Postgres, for example Supabase/Postgres.

For local development, either:

- SQLite for quick iteration, or
- local Postgres/Supabase if we want dev/prod parity.

### Backend

Continue from the existing Flask app unless we choose a larger rewrite.

Suggested responsibilities:

- Admin auth/session handling.
- Member CRUD/imports.
- Match CRUD/imports.
- Check-in endpoint.
- Wallet pass generation.
- Apple/Google update hooks.
- Export/dashboard generation.

### Frontend

Keep it server-rendered and mobile-first:

- Public landing/headcount.
- Admin dashboard.
- Member add/import.
- Match update page.
- Scanner page.
- Pass recovery page.
- Member pass download page.
- Season dashboard page.

## Signup Delivery And Pass Recovery

Owning the pass system gives us a much better member experience than PassKit alone.

### Automatic Pass Delivery

When someone signs up or is imported:

1. Create or update the member in our database.
2. Attach the member to the current season.
3. Generate their credential token.
4. Generate Apple Wallet pass, and later Google Wallet object/link.
5. Send a welcome email with:
   - Apple Wallet link.
   - Google Wallet link when available.
   - Mobile web pass/recovery link.
   - Basic "how to use it at the bar" instructions.

Sources that can trigger this:

- Squarespace webhook.
- CSV import.
- Admin manual add.
- Future member signup form.

The result: no manual line at the bar just because someone joined recently.

### Self-Service Pass Recovery

Add a public page:

- `GET /recover-pass`
- User enters email address.
- App always responds with the same generic message: "If that email is active, we sent your pass link."
- If a current-season member exists, send a fresh pass email.

**Decision (Aug 13-14, not yet built):** deliberately override the
"don't reveal whether an email exists" rule below for the not-found case
specifically. Membership is single-season, so a real, common case is
"you just haven't joined this season yet" — when the lookup fails, tell them
that plainly and point straight at the real purchase link:
`https://olscbrooklyn.com/shop/p/lfc-brooklyn-2627-membership`. This is a
conscious tradeoff (small email-enumeration risk, traded for an actual
conversion opportunity for a small supporters club), not an oversight.

Security rules:

- Do not reveal whether an email exists **for the found case** — a match
  should look identical whether or not they're logged in/aware. The
  not-found case is the deliberate exception above.
- Rate limit by IP and email.
- Log recovery attempts.
- Send only to the email on file.
- Optionally rotate/reissue pass token if abuse is suspected.

Recovery email should include:

- Apple Wallet download link.
- Google Wallet link when available.
- Mobile web pass link.
- Support/contact fallback.

Admin backup:

- Admin can resend a pass from the member detail page.
- Admin can rotate a credential token if a pass was shared/leaked.

This is a major reason to own the system: members can recover passes themselves without tying up the entry line.

## Wallet Strategy

### Apple Wallet

Required:

- Active Apple Developer Program when issuing/updating passes.
- Pass Type ID.
- Pass signing certificate.
- Team ID.
- `.pkpass` generation/signing.
- Hosted pass download links.
- Optional Apple Wallet web service for push updates.

MVP:

- Generate a `.pkpass` with member name, season, QR code, and next match.
- Email/download link from our app.
- Reissue pass when needed.

**Lock-screen relevance (Aug 11):** Apple Wallet passes now include a location
hint for the bar at `481 5th Ave, Brooklyn, NY 11215` (approx.
`40.6657, -73.9877`) with relevant text `Up the Reds.`. Member passes also set
`relevantDate` from the current DB match kickoff when generated, so iOS has
both place and matchday timing signals. This is an Apple-controlled relevance
hint, not a guaranteed custom push notification; future Apple Wallet web
service updates would be needed to keep `relevantDate` fresh without resending
passes.

**Gotcha hit and fixed (Aug 10):** if `APPLE_PASS_CERT_P12_BASE64` is set, the
app uses it and silently ignores `certs/passTypeCert.p12` on disk, even
locally. A stale base64 value left over from earlier testing caused hours of
"invalid password" errors that had nothing to do with the actual password —
every fix to the file itself was being ignored. Lesson: if this ever breaks
again after a cert rotation, check for a stale `APPLE_PASS_CERT_P12_BASE64`
env var before re-debugging the password. Separately: `security export -t
identities` from the CLI is a reliable way to pull a cert+key out of Keychain
without the GUI export dialog (which kept silently producing
mismatched/wrong passwords) — but it bundles *every* identity in the
keychain, so the target one must be isolated by `localKeyID` before
repackaging into a clean single-identity `.p12`.

**Real crest art (Aug 10):** `wallet_pass.py`'s icon/logo generation
(`_make_pass_images`, formerly `_make_placeholder_images`) now uses the
actual OLSC/LFC crest instead of generated "OLSC" text. Source: `Desktop/
Artwork-Assets/Official Logos/OLSC Logos/Mono red.png` — the crest region
was cropped out (isolated from the "Official Supporters Club / Brooklyn"
wordmark below it), recolored white (the source is red-on-transparent; our
pass background is red, so red-on-red would've been invisible), and bundled
into the repo at `wallet_pass_assets/olsc_crest_white.png` since the Desktop
folder it came from won't exist on Render. **Revised same day:** logo was initially crest-only (reasoning:
`organizationName` already renders as text elsewhere in Wallet) — corrected
after feedback that a bare crest can't be told apart from any other
LFC-branded pass someone might have (official club membership, tickets,
etc.). Logo now uses a second extracted asset, the full "crest + Official
Supporters Club / Brooklyn" lockup from `Horizontal_Mono Red.png`
(`wallet_pass_assets/olsc_wordmark_white.png`), so the header itself says
which club/branch this is, not just that it's LFC-related. Icon stays
crest-only (29-87px is too small for the wordmark to be legible anyway).
Still using the `generic` pass style (flat `backgroundColor`) — switching to
`storeCard` for real background artwork (not just a logo) was raised and
left as an open, undecided option for later, not rejected.

**2026/27 home/away kit theming (Aug 10-11):** both the Wallet pass and the
mobile web page automatically switch color scheme based on whether the next
match is home or away, using `is_home` from `get_next_match()` — the exact
same source already driving the "Next Match" text, so the two can never
disagree with each other. Home = red background, white crest/wordmark
(existing look). Away = white background, red crest/wordmark, matching the
actual 2026/27 road jersey (white, red lettering) — deliberately *not* the
green away-kit color `pass_themes.py` used for a prior season's kit, since
kit colors change every year and that file was stale. Required a second set
of bundled assets, `wallet_pass_assets/olsc_crest_red.png` and
`olsc_wordmark_red.png` (same extraction as the white versions, just without
the recolor step, since the source art is already red). Defined once as
`PASS_THEMES` (home/away dict with colors + asset paths) so both `wallet_pass.py`
and `app.py`'s mobile-pass route read from one shared source instead of two
copies that could drift apart. No manual toggle needed — genuinely automatic.

Later:

- Add Apple Wallet web service registration.
- Push updates when the next match changes.

### Google Wallet

Current setup:

- Google Wallet API Issuer account exists.
- Issuer ID: `3388000000023170524`.
- Assume demo mode until publishing access is explicitly requested and approved.
- Code wiring exists in demo mode: pass emails and `/pass/<token>` include an Add to Google Wallet link when `GOOGLE_WALLET_ISSUER_ID` and `GOOGLE_WALLET_SERVICE_ACCOUNT_JSON_BASE64` are configured.
- Google Wallet uses a Generic Pass class/object embedded in a signed Save-to-Wallet JWT. Class/object creation happens just in time when an approved test account taps the link.
- The Google Wallet barcode payload is the same raw database token as Apple Wallet, so the existing `/scanner` endpoint accepts Apple Wallet, Google Wallet, and mobile web QR codes without branching.

Required:

- Service account.
- Invite the service account email as a Developer user in the Google Pay & Wallet Console.
- Wallet class/object creation. Current implementation does this just in time through the signed Save URL.
- Save-to-Google-Wallet link. Current implementation generates this during pass issuance and mobile-pass rendering.
- Publishing access before issuing passes broadly to members.
- Render env vars:
  - `GOOGLE_WALLET_ISSUER_ID=3388000000023170524`
  - `GOOGLE_WALLET_SERVICE_ACCOUNT_JSON_BASE64=<base64 service account JSON>`
  - Optional `GOOGLE_WALLET_CLASS_SUFFIX=<stable class suffix>`
  - Recommended `PUBLIC_BASE_URL=https://<render-app-host>`

Testing gotcha (Aug 13):

- The service account JSON is tied to the Google Cloud project/service account, not directly to the Wallet Issuer ID.
- If Android testing fails with authorization/issuer errors, first verify `GOOGLE_WALLET_ISSUER_ID` in Render is `3388000000023170524` and that the exact service account email from the JSON key has been invited as a **Developer** on that same Wallet issuer.
- A JSON key generated while logged into a different Google account can still be valid if it belongs to the intended Cloud project and its service account has Developer access on the correct Wallet issuer.

Aug 13 debugging handoff:

- User hit `Something went wrong. Please try again.` at `pay.google.com/gp/v/save/...` on desktop Safari while logged into `angusisthenewblack@gmail.com`. This should be a valid test surface; lack of an Android device is not, by itself, the blocker.
- We initially confused two issuer IDs. Treat `3388000000023170524` as the authoritative OLSC Brooklyn Google Wallet issuer ID. Older doc references to `3388000000023188178` were corrected, and Render was reported to match `3388000000023170524`.
- `GOOGLE_WALLET_SERVICE_ACCOUNT_JSON_BASE64` was decoded locally with a helper script. It is valid base64 JSON. It contains:
  - `client_email`: `olsc-wallet-pass-issuer@olsc-digital-id.iam.gserviceaccount.com`
  - `project_id`: `olsc-digital-id`
- Do not regenerate the base64 key just because the issuer ID changed. Regenerate only if switching service accounts/keys or if the current service account cannot be authorized on the correct issuer.
- User confirmed the service account already appears in the Google Pay & Wallet Console user invite flow with **Developer** access (`The user has already been added.`). The human Gmail was also added to test accounts. Do not keep looping on the same basic permission instructions unless new evidence shows the wrong issuer/account is selected.
- We pushed commit `6b1a9f8` to make the just-in-time Generic Class match Google's sample more closely: `generic_class = {"id": class_id}` only, with default class suffix bumped to `_v2` to avoid reusing a possibly bad first demo class. Old save links remain invalid; only fresh links generated after Render deploy can test this.
- Current implementation still uses the signed Save-to-Wallet JWT path with embedded `genericClasses` and `genericObjects`, not explicit REST API class/object creation first.
- Recommended next step for Claude: stop guessing from the generic Google error page. Capture/decode the fresh signed JWT payload from the failing `pay.google.com/gp/v/save/...` link and verify issuer ID, class ID, object ID, service account `iss`, `origins`, logo URL, and barcode payload. If payload looks correct, use the Google Wallet REST API with the same service account to create/get the Generic Class/Object directly so Google returns a real API error instead of the opaque browser message.

MVP can come after Apple Wallet if most members use iPhone.

**Decision (Aug 7):** Google Wallet is in scope for launch, not deferred
indefinitely — but it does not gate the Aug 12 Alamo checkpoint. Apple
Wallet + DB + scanner must be green by Aug 12; Google Wallet is a fast-follow
immediately after, targeted for the Aug 13-16 window, so Android members
aren't left with only a mobile-web fallback for long. If Google Wallet isn't
ready by Aug 17-18, launch proceeds Apple-first with the mobile-web QR page
covering Android in the interim, rather than falling back to PassKit over
this alone.

## Pass Content Ideas (discussed, not built)

Brainstormed for the back of the Apple Wallet pass / mobile web page.
None of this is built — captured so the ideas aren't lost to scrollback.

- **Favorite player + their current-season goals.** Real data exists —
  `orders.csv` (Squarespace export) has a "favorite player" checkout
  question, free text, tied to email, but not in our `members` table yet
  and only answered on ~9 of however-many total orders (question may not
  have existed on older signup forms). Goals-per-player is genuinely free
  via football-data.org's existing `scorers` endpoint (same API key
  already in use). Assists are not free — that needs their paid "Deep
  Data" plan (€29/mo), so drop assists. Two real blockers if this gets
  built: (1) staleness — goal totals change multiple times a week
  regardless of what a member does, so this must live on the mobile web
  page (rendered fresh each time), never baked into the static `.pkpass`;
  (2) name matching — free-text answers ("Mo," "Szobo," "Curtis
  Jones!!!!") won't reliably auto-match to real API player names, so this
  needs a manual admin mapping step, not a fully automatic pipeline.
  Goalkeepers/defenders are usually a near-empty stat under a pure-goals
  model (no free per-player clean-sheet/save data exists) — the discussed
  fallback is: omit the stat line entirely when there's no real entry for
  that player, don't show "0 goals."
- **All-time attendance count / "first match ever attended."** Not
  possible with current data — our `checkins` table only has depth from
  when this system went live; there's no imported check-in history from
  the PassKit era to compute this from.
- **Full season-by-season membership history** ("member since 2023,"
  every season attended) — this one *is* buildable now from
  `member_seasons`, no new data needed, whenever there's time.
- **A "member number"** (just `members.id`, framed as "Member No. 0047")
  — free, buildable, discussed as a fun exclusivity touch.
- **ASCII art on the back of the pass.** Real open question, not decided:
  Apple renders back fields in a proportional system font (San Francisco),
  not monospace, so precise multi-line ASCII art will likely warp — simple
  single-line stuff has a real shot, anything alignment-dependent probably
  doesn't survive. Not yet spike-tested on a real device; cheap to just
  try once someone wants an answer instead of guessing.
- **Chant/song lyrics for a member's favorite player.** Raised, declined —
  real chant lyrics are copyrighted and can't be reproduced (including
  close paraphrases). Alternative that stays legally clean: original,
  factual terrace-style text we write ourselves (e.g. referencing a real
  moment/date, not a song), or original text OLSC Brooklyn writes itself
  rather than an existing copyrighted chant.

## QR Credential Design

Each member gets a random, unguessable credential token.

Apple Wallet QR payload:

```text
<random-token>
```

Mobile web pass QR payload:

```text
https://olsc.example.com/checkin/t/<random-token>
```

Rules:

- Token should not expose email or member ID.
- Token should be long enough to resist guessing.
- Token can be rotated if a pass is leaked.
- Server hashes the token, looks it up, and records the check-in.
- Admin scanner accepts either raw tokens or `/checkin/t/<token>` URLs and submits the token to an authenticated endpoint.

### Barcode Format

QR is the recommended default because it can hold a URL/token payload and scans reliably with phone cameras.

Other possible formats:

- Aztec
- PDF417
- Code 128

Apple Wallet supports multiple barcode formats, but QR is the simplest fit for a web-based scanner.

### Rotating Codes

Rotating codes are possible, but not in the same way inside Wallet passes.

Important constraint:

- Apple Wallet and Google Wallet passes cannot run our JavaScript or generate a new barcode every 30 seconds locally.
- A barcode inside a Wallet pass is static until the pass is updated by our server.
- Server-pushing a new barcode every 30-60 seconds to every member is not a good design.

Recommended approach:

- Use a static, unguessable Wallet token for the normal pass.
- Make check-ins idempotent per member/match so screenshots cannot create multiple check-ins.
- Show member name/status on scan so admins can catch obvious misuse.
- Allow token rotation/revocation if a pass is leaked.

Higher-security option:

- Add a mobile web "live credential" page that generates a short-lived QR/barcode from a per-member secret.
- The Wallet pass can link to that page.
- The scanner verifies the time-windowed code.

Tradeoff:

- Static Wallet QR is fastest and most reliable at the bar.
- Rotating web QR is more secure, but members must open a web page instead of only showing Wallet.

## Admin-Gated Scanner

Yes, scanning can be done through a web page with an admin gate.

### Scanner UX

Admin flow:

1. Admin logs in.
2. Admin opens `/scanner`.
3. Browser asks for camera permission.
4. Admin scans the QR code from Apple Wallet, Google Wallet, or mobile web pass.
5. Page shows a big success/failure state:
   - Member name.
   - Match.
   - Already checked in vs newly checked in.
   - Timestamp.
6. Scanner returns immediately to scan mode.

### Door Staff Result States

The scanner should make status obvious at arm's length.

Recommended states:

- **New valid check-in:** green success state, member name, match, timestamp.
- **Already checked in today / this match:** strong warning state, member name, original check-in time, and "Already used for this match."
- **Invalid token:** red error state with "Invalid pass."
- **Expired/old-season pass:** red error state with "Expired pass."
- **Wrong match/manual review:** amber warning state with admin override option.

For "already used" we should not rely on subtle copy. Door staff should see a distinct color, a large label, and the prior scan time.

### Visual Authenticity Cues

We can add animated/liveness styling to web pages we control:

- Green animated success border after a fresh scan.
- Streaming color border on an optional member live-credential page.
- Bouncing footballs or club-themed motion on the live credential page.
- Large changing timestamp or short code on the live credential page.

Important limitation:

- Apple Wallet and Google Wallet passes themselves are static. They cannot show bouncing footballs or animated borders.
- The admin scanner page can animate scan results.
- A mobile web credential page can animate as a stronger anti-screenshot signal.
- The server-side scan result is still authoritative; animation is a helpful visual cue, not security by itself.

### Scanner Endpoints

Suggested routes:

- `GET /scanner`
  - Admin-only page.
  - Uses phone camera through browser APIs.

- `POST /api/checkins/scan`
  - Admin-only endpoint.
  - Body: `{ "token": "...", "match_id": "..." }`
  - Creates or returns a check-in.

- `GET /api/matches/current`
  - Admin-only or public depending on use.
  - Returns current match for scanner default.

### Scanner Security

- Scanner page must require admin login.
- Check-in API must require admin session.
- QR token must be unguessable.
- Check-in should be idempotent per member/match.
- Log every scan attempt.
- Rate limit invalid tokens.
- Show limited member data on scanner result.

### Scanner Implementation Options

Browser camera libraries:

- `html5-qrcode`
- `zxing-js/browser`

Use whichever works best on mobile Safari and Chrome.

Important browser notes:

- Camera access requires HTTPS in production.
- iOS Safari camera behavior should be tested early.
- The scanner page should have a manual token/member search fallback in case camera access fails.

**Bug found and fixed (Aug 16):** the manual-paste fallback only ever correctly
parsed a bare token or a `/checkin/t/<token>` URL (the shape embedded in the
QR itself) — but that's not what a person would ever realistically have as
copyable text. The one link that's actually in emails/the mobile pass page
(`/pass/<token>`) silently failed to parse and would never have checked
anyone in. Fixed `_extract_scan_token()` to also recognize `/pass/<token>`.
Verified end-to-end: pasting a real `/pass/<token>` link now produces a real
check-in, not just correct parsing in isolation.

**Visual redesign (Aug 13):** `/scanner` restyled to match the crest/wordmark
design language used everywhere else (previously plain black header, old
red-green gradient body). Now theme-aware (`is_home`, same source as the
pass and mobile web page — home/away kit colors, no green anywhere in the
app anymore) and auto-starts the camera on page load so door staff don't
need an extra tap every time it opens (falls back silently to the Start
button if the browser blocks camera access without a user gesture). Visual/
layout only — every element ID and class the scan logic depends on (`#result`,
`#cameraWrap`, `#reader`, `#startBtn`, `#stopBtn`, `#manualToken`, `#manualBtn`,
the success/warning/error state classes) was left untouched, verified via
test client before and after.

**Idea raised and ruled out (Aug 13):** "bump two iPhones together" to
authenticate a lost-pass member at the door and trigger a resend. Not
feasible — AirDrop and Apple's actual proximity-identity feature ("Tap to
Present ID") are both OS-level and require a native app with special Apple
business entitlements; a web app has no access to either. Web NFC doesn't
exist on iOS Safari at all (Android-Chrome-only, and read-only for physical
tags even there). The old "Bump" app (~2010) wasn't real hardware handshake
either — it matched two phones' accelerometer/timestamp reports via a
server, which is a weak identity signal even if rebuilt (proves two phones
moved at the same moment, not who anyone is). Real answer for a lost pass:
self-service recovery or a fresh purchase link — see "Next up" at the top
and Self-Service Pass Recovery below.

## Check-In Rules

Recommended rule:

- One check-in per `member_id + match_id`.
- If scanned again, show “Already checked in” with original time.
- Allow admin override only from an admin detail page.

Recommended fields:

- `id`
- `member_id`
- `match_id`
- `checked_in_at`
- `scanner_admin_id`
- `source` (`scanner`, `manual`, `import`)
- `raw_token_hash`
- `notes`

### Future Idea: Match-Day Trivia

Door staff sometimes ask a question of the day at check-in (e.g. "what minute
will the winner be scored in?"). Worth capturing and analyzing later.

Recommended design when this gets built — keep it separate from `checkins`,
not bolted onto it, since a check-in ("did they show up") and a trivia answer
("what did they guess") are different concerns with different lifecycles
(answers get scored after the match; check-ins don't):

- `trivia_questions`: `id`, `match_id`, `question_text`, `correct_answer` (filled in after the match).
- `trivia_answers`: `id`, `member_id`, `match_id`, `question_id`, `answer_value`, `submitted_at`, `is_correct` (set once scored).

This unlocks season-long stuff like "best predictor of the year" without
tangling it into attendance data. Not needed for launch — two-table addition
whenever there's time post-August.

## Match Model

Each check-in should attach to a match record.

Match fields:

- opponent
- home/away
- competition
- kickoff time
- venue/bar location
- final score
- season
- external source ID if imported

Current match selection:

- Default scanner to the next/upcoming match.
- After kickoff, scanner should use the match happening today.
- Admin can override the current match.

## Season Exports And Dashboard

Exports should come from our database:

- all members
- all check-ins
- all matches
- top attendees
- busiest matches
- last-match CSV
- wallet issue/update status

The existing `export_passkit_season.py` is a good reference for output format and dashboard style, but the future exporter should read from our DB instead of PassKit.

## Migration Path From Current App

### Phase 0: Preserve Current State

- Keep `OFFSEASON_RESTART_MANUAL.md`.
- Keep `export_passkit_season.py`.
- Keep final `season_exports/` archive outside git.
- Keep current Flask app as reference.

### Barebones Launch Target

Start with the cheapest useful version and upgrade only when the workflow proves itself.

Barebones scope:

- Use the existing Flask app as the base.
- Use a free managed Postgres tier if available; do not rely on server disk for production check-ins.
- Launch on the existing HTTPS Render URL first, unless the custom domain is already confirmed and ready.
- Admin login.
- Member table.
- Match table.
- Admin scanner page.
- Static unguessable QR token per member.
- Apple Wallet pass MVP.
- Manual or simple email pass delivery.
- CSV export for members/check-ins.

Render plan strategy:

- Keep `olsc-web-app` on Render free tier during build/stabilization.
- Free tier is acceptable while testing Wallet downloads, DB writes, scanner flow, and exports.
- Upgrade to an always-on paid Render instance only before real match-day use, when door speed matters.
- The reason to upgrade is cold starts, not expected traffic volume or rate limits.
- Avoid enabling higher-performance build pipelines unless a normal build actually fails.

### August 2026 Launch Timeline

**Corrected Aug 10:** the real constraint isn't a fixed calendar target, it's
two dates specific to this year: **Friday, August 21** is the last day with
dedicated work time before travel back to the States, and **Sunday, August
23** is the actual first match (Newcastle) where door staff would need to
scan a pass for real. The originally-drafted Aug 17-18 target and Aug 12
Alamo checkpoint were placeholders written before those two dates were
known — replaced below.

Target: live, functional, and in members' wallets by **Friday, August 21**
(the last day there's time to fix anything before travel), with a 2-day
buffer before the **Sunday, August 23** first match.

Because the fallback is another PassKit season, the cut-bait date must leave enough time to restart PassKit, import members, test passes, and send links — and it must land before travel starts, not after.

#### Hard Alamo Checkpoint

**Wednesday, August 19, 2026, end of day New York time.**

If the required gates below are not met by then, switch to PassKit for one more season. This leaves Aug 20-21 to execute a PassKit restart while there's still work time available, and Aug 22 as a buffer day before the Aug 23 match.

Latest possible stretch checkpoint:

**Thursday, August 20, 2026, noon New York time.**

Only use the stretch checkpoint if everything works except a small fix. Do not use it if Apple Wallet signing/installing or check-in persistence is still unresolved. Do not push the stretch checkpoint past Aug 20 — Aug 21 is needed as a landing day before travel, not more build time.

#### Go/No-Go Gates

By the Alamo checkpoint, all of these must be true:

- Production URL works over HTTPS. Existing Render HTTPS is acceptable for launch; hosted Wallet pass download has been verified on Render.
- Database is live and check-ins survive deploys/restarts.
- Admin login works in production.
- Members can be added/imported.
- A real Apple Wallet pass can be generated and installed on at least two iPhones.
- A mobile web pass page exists (name, season, QR) and its QR scans correctly — this is the real launch-day path for Android, not a nice-to-have.
- The pass QR/barcode scans from the admin `/scanner` page, whether it came from Apple Wallet or the mobile web page.
- A fresh scan creates a check-in tied to the current match.
- A duplicate scan clearly shows "Already used for this match" with original timestamp.
- Last-match/check-in CSV export works.
- There is a documented member send/re-send flow.

Automatic PassKit fallback if any of these are still broken:

- Apple Wallet pass cannot be installed from the production domain. Initial hardcoded install spike passed; hosted Render Wallet pass path is now verified.
- Pass signing/certificates are not working. Initial signing/cert chain spike passed.
- Scanner cannot reliably use the phone camera on iPhone.
- The mobile web pass page doesn't work — this leaves Android members with no way to get a scannable credential at all.
- Check-ins are not persisted in the DB.
- We cannot import the member list quickly.
- Admin auth is unreliable.

Fallback implementation note:

- Keep PassKit routes/scripts in the repo until after the season launch is stable.
- Normal `/add-member` redirects to the new DB-backed `/admin/members` flow to avoid accidental expired-account calls.
- Emergency PassKit add-member UI remains available at `/legacy/passkit/add-member` if we cut bait and reactivate PassKit.

#### Suggested Daily Milestones (revised Aug 10)

- **Aug 6-7:** Apple Developer status, Pass Type ID/cert, hardcoded `.pkpass` generated, signed, hosted, and installed on a real iPhone from an existing HTTPS URL. **Done: Apple Wallet spike succeeded on a real iPhone.**
- **Aug 8:** Free managed Postgres, one plain `schema.sql`, real member data wired into the working pass generator. **Done.**
- **Aug 9:** Admin member/match CRUD and import on top of existing auth. **Done, tested locally against live Supabase.**
- **Aug 10 (today):** Lost most of this day to an Apple Wallet cert/env-var debugging saga (stale `APPLE_PASS_CERT_P12_BASE64` silently overriding the real cert file — see the "Gotcha hit and fixed" note under Apple Wallet). **Done: production `/wallet/test-pass.pkpass` on Render now generates a real signed pass with live next-match data — the hosted delivery path is fully proven.**
- **Aug 11-13:** Scanner page + `POST /api/checkins/scan` (idempotent per member/match), plus the mobile web pass page (Android's launch-day path). Both fully unstarted as of Aug 10 — this is the real remaining core of the project.
- **Aug 14-15:** Deploy DB/admin/scanner routes to Render and verify end-to-end in production (not just local+Supabase): add a member, generate/install a pass on a 2nd iPhone, scan it, confirm duplicate-scan warning, check the mobile web page on an Android-like browser.
- **Aug 16-18:** CSV export, resend-flow end-to-end test with Resend, staff-facing runbook, polish, buffer for whatever breaks.
- **Aug 19:** Alamo go/no-go.
- **Aug 20:** Stretch checkpoint (small fixes only) if not fully green on the 19th.
- **Aug 20-21:** If green — member import, final polish, welcome emails. If red — execute PassKit fallback (restart subscription, import members, send links) while there's still work time.
- **Aug 21:** Last work day before travel — everything must be in its final state by end of this day.
- **Aug 23:** First match (Newcastle) — the actual first real-world test at the door.

Defer:

- Custom `redcrowlabs.com` domain if DNS/cert setup is not already trivial.
- Push updates.
- Rotating live credentials.
- Automated Squarespace webhook.
- Public pass recovery flow.
- Formal migration framework.
- Repository/service abstraction.
- SMS/reminders.
- Fancy dashboard polish.

Barebones success criteria:

- An admin can add/import members.
- A member can install an Apple Wallet pass.
- Door staff can scan the pass from a phone browser.
- A duplicate scan shows "Already used for this match."
- Check-ins survive deploys/restarts.
- We can export the match attendance CSV.

### Domain Plan

`redcrowlabs.com` can work as the hosting domain if we control DNS.

Do not put custom domain setup on the August launch critical path. Use the existing Render HTTPS domain for the MVP unless DNS and certificate setup are already confirmed and fast.

Recommended subdomain:

- `olsc.redcrowlabs.com`

Alternative subdomains:

- `passes.redcrowlabs.com`
- `olscbk.redcrowlabs.com`
- `scanner.redcrowlabs.com`

Use one primary subdomain if possible. It keeps Apple pass links, admin login, scanner, and member pages simpler.

Requirements:

- HTTPS must be enabled.
- DNS points to the hosting provider.
- The domain should be stable for the whole season.
- Apple Wallet pass links and any future pass web service URLs should use this domain.

### Phase 1: Database Foundation

- Add DB connection.
- Add one plain `schema.sql` for seasons, members, matches, check-ins, passes.
- Apply schema manually for MVP.
- Avoid repository/service abstraction until the MVP works.

**Done (Aug 6-7):** Dedicated free-tier Supabase project created (separate
account, to avoid the org's 2-project free-tier cap). `schema.sql` and `db.py`
added — six tables (`seasons`, `members`, `member_seasons`, `matches`,
`checkins`, `wallet_passes`), no migration framework. Schema applied and
`2026/27` seeded as the current season. `imports` and `admin_audit_log` from
the original architecture sketch were dropped for MVP — not launch-blocking.

### Phase 2: Admin Members And Matches

- Admin member list.
- Add/edit/import members.
- Match list.
- Current match selector.

**Done (Aug 9):** `/admin/members` and `/admin/matches` routes added to
`app.py`, separate from the PassKit-backed add-member/update-match routes.
CSV import accepts both a plain `first_name,last_name,email,phone` format and
raw PassKit exports (`person.displayName`/`person.emailAddress`/`person.mobileNumber`).
Re-importing an existing email attaches them to the current season instead of
duplicating. One bug caught and fixed during testing: match kickoff times were
displaying in UTC instead of `America/New_York` (data was stored correctly;
only the admin display was wrong) — fixed by converting with `pytz` before
rendering.

**Done (Aug 9, resend/re-issue flow):** `db.issue_wallet_token()` added —
issues a token, stores only its SHA-256 hash in `wallet_passes`, returns the
raw token once for immediate use. Deliberately no way to recover a
previously-issued token: every "Resend Pass" click rotates the credential and
invalidates whatever pass existed before (safe default for a
screenshotted/shared pass). "Resend Pass" button added per member on
`/admin/members`, wired to a new route that issues a token, calls Cursor's
`wallet_pass.build_member_pkpass()` with the member's real name/season, and
emails the signed `.pkpass` as an attachment. Email now uses Resend first
(`RESEND_API_KEY`, default sender `OLSC Brooklyn <DIGITALIDS@OLSCBROOKLYN.COM>`,
default reply-to `OLSC_BK@olscbrooklyn.com`) with SMTP fallback. Tested: DB rotation
logic (verified only one row per member/season/platform, hash changes, old
token stops matching), and the route's error handling (confirmed it fails
gracefully with a clear on-page message and no secrets leaked when Apple
Wallet isn't fully configured). Resend plumbing is deployed but still needs
a real member email send verified after Render finishes redeploying.

### Phase 3: Scanner

- Add `/scanner` page.
- Add QR camera support.
- Add `POST /api/checkins/scan`.
- Store check-ins in DB.
- Add manual fallback.

### Phase 4: Apple Wallet MVP

- Generate Apple `.pkpass` files.
- Sign passes.
- Show pass download link.
- Email welcome/pass link.
- QR token links back to our scanner/check-in flow.

### Phase 5: Google Wallet MVP

- Post-launch.
- Issuer ID obtained: `3388000000023170524`.
- Set up Google Cloud service account and Render env vars.
- Create Google Wallet class/object only after Apple Wallet launch is stable.
- Generate Save-to-Google-Wallet links.
- Keep QR/check-in behavior identical.

### Phase 6: Pass Updates

- Update next match on pass.
- Add Apple push update support.
- Add Google object updates.

### Phase 7: Season Dashboard

- DB-backed dashboard.
- Last-match CSV export.
- Season archive command.

## Open Questions

- Do we want Supabase/Postgres or another hosted database?
- Are most members on Apple Wallet, Google Wallet, or mixed?
- Do we need Google Wallet for launch, or can Apple + mobile web pass launch first?
- Who should have scanner/admin access?
- Should check-in require selecting a match, or always use current match?
- Do we need offline scan support at the bar?
- Should members be able to self-serve pass re-downloads?

## Suggested Repo Workstreams

For the August sprint, build sequentially instead of splitting into formal PR workstreams.

1. `apple-wallet-spike`
   - Hardcoded member.
   - Generate/sign/install `.pkpass`.
   - This is the first go/no-go risk.

2. `db-schema`
   - Add DB config and one plain schema.

3. `admin-members-matches`
   - Add member/match admin screens backed by DB.

4. `scanner`
   - Add camera scanner and check-in endpoints.

5. `season-dashboard`
   - Add analytics/export views from DB.

Post-launch:

- Google Wallet pass issuing.
- Custom domain.
- Public pass recovery.
- Squarespace webhook automation.

## Claude Collaboration Notes

This doc is safe to share with another coding assistant as project context.

Recommended Claude task prompts:

### Database Schema Task

Review `SELF_HOSTED_WALLET_PLAN.md` and the existing Flask app. Propose a minimal Postgres schema and migration plan for seasons, members, matches, wallet passes, check-ins, imports, and audit logs. Keep PassKit out of the new data model except as optional legacy external IDs.

### Scanner Task

Review `SELF_HOSTED_WALLET_PLAN.md` and the existing Flask app auth/session patterns. Design and implement an admin-only `/scanner` page using a mobile-browser QR scanner library, plus an idempotent `POST /api/checkins/scan` endpoint. Include a manual fallback for camera failures.

### Apple Wallet Task

Review `SELF_HOSTED_WALLET_PLAN.md`. Research the current Apple Wallet pass requirements, then propose the exact files/configuration needed to generate and sign `.pkpass` membership cards from Flask. Do not commit certificates or secrets.

### Export/Dashboard Task

Review `export_passkit_season.py` and `SELF_HOSTED_WALLET_PLAN.md`. Design a DB-backed replacement that exports members, matches, check-ins, top attendees, busiest matches, and last-match CSVs without calling PassKit.

## Decision Recommendation

Build this.

Apple Wallet viability is no longer an open question for this season: the hardcoded `.pkpass` signing/install spike succeeded with the active Apple Developer account, Team ID, Pass Type ID, and certificate chain.

The cost savings are worth it if we keep scope tight:

1. DB-owned members/check-ins.
2. Admin web scanner.
3. Apple Wallet MVP.
4. Google Wallet after Apple works.

Do not start by cloning every PassKit feature. Start by owning member identity and check-in history.

## Expected Costs

The cash cost should be much lower than PassKit. The real cost is implementation and maintenance time.

### Required / Likely

| Item | Expected Cost | Notes |
| --- | ---: | --- |
| Apple Developer Program | ~$99/year | Required for Apple Wallet pass certificates and issuing/updating passes. |
| Google Wallet issuer/API | $0 | Google Wallet APIs generally do not add a direct per-pass fee. |
| Hosting | $0-$25/month | Existing Render/Vercel setup may work. Paid hosting avoids sleep/cold starts. |
| Database | $0-$25/month | Supabase/Postgres free tier may be enough; paid tier is better for backups/reliability. |
| Domain | $0-$20/year | Only if we need a new domain/subdomain setup. |
| Email sending | $0-$15/month | Existing SMTP may work; Postmark/SendGrid/etc. if we want reliable delivery. |
| Football-data.org | $0+ | Existing free/API setup may be enough for match lookups. |

### Optional

| Item | Expected Cost | Notes |
| --- | ---: | --- |
| Error monitoring | $0-$25/month | Sentry/etc.; useful but not required at launch. |
| Backups/storage | $0-$10/month | Usually covered by existing cloud storage or DB provider. |
| SMS | Usage-based | Only if we add Twilio/text reminders later. |

### Practical Annual Ranges

- **Bare bones:** about `$99/year` plus existing hosting/tools.
- **Comfortable small production:** about `$200-$500/year`.
- **More polished/reliable:** about `$500-$900/year`.

Even the polished version should be far below PassKit if PassKit remains the largest club software expense.
